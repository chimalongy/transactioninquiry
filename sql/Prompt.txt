hello, I want you to review this code base, design and structure

httpsgithub.comchimalongyptsamonitor 

following the same apprroach in this ptsamonitor repo,

modify this new project

httpsgithub.comchimalongytransactioninquiry 

such that 

a user is created by the admin with a default password, the user MUST update their password before they can login into the dashbaord

the dashboard should have the following tabs
the indexpage
SWGLOBAL
SETTLEMENT REPORTS
SCRIPTS
MANUAL SETTLEMENTS
EXCEPTION REPORTS
USERS
AUDIT

HERE ARE THE USERS AND AUDITS TABLES

users table

CREATE TABLE transactionInquiryUsers (
    id BIGSERIAL PRIMARY KEY,
    firstName VARCHAR(100) NOT NULL,
    lastName VARCHAR(100) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    accountStatus VARCHAR(50) NOT NULL,
    accountType VARCHAR(50) NOT NULL,
    privileges TEXT,
    lastLoginIP VARCHAR(45),
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);


auditlogs table

CREATE TABLE auditLogs (
    id BIGSERIAL PRIMARY KEY,
    account VARCHAR(255) NOT NULL,
    action VARCHAR(100) NOT NULL,
    description TEXT,
    ipAddress VARCHAR(45),
    time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

THE DATABASE FOR WEHRE THE USERS ARE STORED  IS IS POSTGRESS NOT ORACLE SO THE ENTITY FRAMEWORK SHOULD WORK WITH POSTGRES

USE JWT AND USE THE SAME CRYPTOR CLASS TO ENCRYPT AND DECRYPT THE DATABASE CONNECTION STRING






